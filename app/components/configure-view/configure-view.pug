.container
  h3.header-title Data Pen
    span.header-subtitle New project
  .project-setup-container.col-md-10.col-md-offset-1
    .row
      .header
        h4.col-md-3.col-md-offset-1 Start a new project.
    .left-side.col-md-4.col-md-offset-1
      citable-editor(citable="$ctrl.project",no-id)
    .right-side.col-md-4.col-md-offset-1
      h5 Pull from an existing project
      p Start with an existing sources and/or filter configuration from another project (optional).
      .project-selection
        label Project:  
        .project-dropdown-container(ng-controller="projectsListController")
          select(ng-model="item.id" ng-selected="0" ng-options="option.id as option.name for option in options")
            option(ng-repeat="option in data.availableOptions" value="{{option.id}}") {{option.name}}
        .project-pull-options
          .label-checkbox-container
            label Sources
              input(type="checkbox")
          .label-checkbox-container
            label Filters
              input(type="checkbox")
          .label-checkbox-container
            label Data
              input(type="checkbox")
    .action-items
      button.save-project(ng-click="$ctrl.saveAndOpen()") Save and open
      button.cancel-project(ng-click="$ctrl.delete()") Cancel
